vastai-sdk>=0.3.0
# Listed explicitly even though current SDK main pulls them as direct
# deps — published wheels lag, and pinning here keeps the example
# self-contained against whatever SDK release is on PyPI.
fastapi>=0.110
uvicorn[standard]>=0.24
