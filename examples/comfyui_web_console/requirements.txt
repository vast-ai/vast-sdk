vastai-sdk
# Listed explicitly because current published vastai-sdk wheels
# don't pull these in transitively. Once a future SDK release
# declares them as direct deps these lines become redundant —
# harmless to leave.
fastapi>=0.110
uvicorn[standard]>=0.24
